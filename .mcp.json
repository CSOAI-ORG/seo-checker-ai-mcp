{
  "name": "seo-checker-ai-mcp",
  "description": "> **By [MEOK AI Labs](https://meok.ai)** \u2014 MEOK AI Labs MCP Server",
  "version": "1.0.0",
  "tools": [
    {
      "name": "analyze_title",
      "description": "Analyze a page title for SEO best practices.",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "target_keyword": {
            "type": "string"
          }
        },
        "required": [
          "title"
        ]
      }
    },
    {
      "name": "check_meta_description",
      "description": "Check a meta description for SEO best practices.",
      "parameters": {
        "type": "object",
        "properties": {
          "description": {
            "type": "string"
          },
          "target_keyword": {
            "type": "string"
          }
        },
        "required": [
          "description"
        ]
      }
    },
    {
      "name": "validate_schema_markup",
      "description": "Validate JSON-LD structured data/schema markup.",
      "parameters": {
        "type": "object",
        "properties": {
          "json_ld": {
            "type": "string"
          }
        },
        "required": [
          "json_ld"
        ]
      }
    },
    {
      "name": "heading_analysis",
      "description": "Analyze heading structure for SEO optimization.",
      "parameters": {
        "type": "object",
        "properties": {
          "html": {
            "type": "string"
          },
          "target_keyword": {
            "type": "string"
          }
        },
        "required": [
          "html"
        ]
      }
    }
  ]
}